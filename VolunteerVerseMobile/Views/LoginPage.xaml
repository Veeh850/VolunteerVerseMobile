<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="VolunteerVerseMobile.Views.LoginPage"
             xmlns:viewmodel ="clr-namespace:VolunteerVerseMobile.ViewModels"
             x:DataType="viewmodel:LoginViewModel"
             Title="LoginPage">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Label Text="Email:" Grid.Row="0"/>
        <Entry x:Name="EmailEntry" Placeholder="Email Address" Grid.Row="0" Grid.Column="1" Text="{Binding EmailAddress}"/>

        <Label Text="Password:" Grid.Row="1"/>
        <Entry x:Name="PasswordEntry" Placeholder="Password" IsPassword="True" Grid.Row="1" Grid.Column="1" Text="{Binding Password}"/>

        <Button Text="Login" Command="{Binding LoginCommand}"  Grid.Row="2" Grid.ColumnSpan="2"/>

        <Label Grid.Row="3" Grid.ColumnSpan="2" Text="{Binding Error}"  TextColor="Red"/>
    </Grid>

</ContentPage>